spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/sql-db
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password:
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
  mail:
    default-encoding: UTF-8
    host: smtp.gmail.com
    username: nava.arul@gmail.com
    password: ThatsTam1l
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    protocol: smtp
    test-connection: false