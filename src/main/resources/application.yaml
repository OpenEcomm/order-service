jasypt:
  encryptor:
    password: openbasketsecret
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sql-db
    driverClassName: com.mysql.jdbc.Driver
    username: root
    password:
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
  mail:
    default-encoding: UTF-8
    host: smtp.gmail.com
    username: project.openbasket@gmail.com
    password: ENC(U6sV6o0flVm5VYuj457AmN4j9JUfAbD2)
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    protocol: smtp
    test-connection: false